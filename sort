#include<stdio.h>
int main()
{
	int n,a[10],i,temp=0,j;
	printf("enter how many elements");
	scanf("%d",&n);
	printf("enter the array of elements");
	for(i=0;i<n;i++)
	{
	scanf("%d",&a[i]);
}
	temp=a[0];
	a[0]=a[n-1];
	a[n-1]=temp;
	for(i=0;i<n;i++)
	{
		printf("%d",a[i]);
	}
	return 0;
}
